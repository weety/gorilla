from building import *

cwd  = GetCurrentDir()
src = Split('''
sensor/sensor.c
moto/pwm.c
moto/moto.c
mpdc/mpdc.c
''')

CPPPATH = [cwd + '/sensor', cwd + '/moto', cwd + '/mpdc']

group = DefineGroup('Gorilla', src, depend = [''], CPPPATH = CPPPATH)

Return('group')

